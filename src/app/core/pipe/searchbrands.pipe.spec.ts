import { SearchbrandsPipe } from './searchbrands.pipe';

describe('SearchbrandsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbrandsPipe();
    expect(pipe).toBeTruthy();
  });
});
